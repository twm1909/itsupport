# Secrets / local config
.env
.env.*
*.key
*.pem
*.p12
.claude/settings.local.json

# Playwright MCP working directory
.playwright-mcp/

# Local skill tooling (installed via `npx skills`) — not part of the deliverable.
# Kept local: the .claude/skills entries are symlinks that don't survive a
# git-for-Windows clone.
.agents/
.claude/skills/
skills-lock.json

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
